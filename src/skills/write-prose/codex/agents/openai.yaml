interface:
  display_name: "Write Prose"
  short_description: "Critical information to read before writing prose (e.g READMEs, articles, docs, etc)"
  default_prompt: "Use $write-prose to write effective human-sounding prose."
