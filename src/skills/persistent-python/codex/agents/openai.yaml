interface:
  display_name: "Persistent Python"
  short_description: "Use a persistent IPython workspace as the default tool bus"
  default_prompt: "Use $persistent-python to work through the persistent IPython tool workspace."
